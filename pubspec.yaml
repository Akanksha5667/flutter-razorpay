name: razorpay_app

description: A new Flutter project to integrate Razorpay.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # http: ^0.15.0
  razorpay_flutter: ^1.3.5
  # flutter_dotenv: ^5.0.2
  fluttertoast: ^8.2.12
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/